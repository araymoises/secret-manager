{
  "name": "secret-manager",
  "version": "1.2.0",
  "description": "Example library as a initial structure for new libraries.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "typings": "./dist/index.d.ts",
  "scripts": {
    "build": "rm -rf ./dist && tsc",
    "release": "node dist/scripts/release",
    "test": "jest --detectOpenHandles --forceExit  --config ./test/jest.config.json",
    "test:cov": "jest --detectOpenHandles --forceExit  --coverage --verbose --config ./test/jest.config.json"
  },
  "author": "",
  "license": "ISC",
  "files": [
    "dist"
  ],
  "dependencies": {
    "@google-cloud/secret-manager": "^4.2.0"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.3",
    "@types/jest": "^29.4.0",
    "@types/node": "^18.11.14",
    "@types/standard-version": "^7.0.1",
    "inquirer": "^8.0.0",
    "jest": "^29.4.3",
    "standard-version": "^9.5.0",
    "ts-jest": "^29.0.5",
    "typescript": "^4.9.5"
  }
}
